[tools]
jq = "1.7.1"
ruby = "3.3"
bun = "1.3.14"
java = "temurin-21"

[tasks."docs:install"]
run = "cd docs && bundle"

[tasks."docs:dev"]
depends = ["docs:install"]
run = "cd docs && bundle exec jekyll serve --watch --incremental --livereload"
