{{#each relationships}}{{#if (eq relationshipType "incoming")}}{{pascalCase (removeSpaces @key)}} {{#if archetypeRelationship}} {{{archetypeRelationship}}} {{else}} -> {{/if}}{{pascalCase (removeSpaces ../elementName)}}{{else}}{{pascalCase (removeSpaces ../elementName)}}{{#if archetypeRelationship}} {{{archetypeRelationship}}} {{else}} -> {{/if}}{{pascalCase (removeSpaces @key)}}{{/if}}{{#unless archetypeRelationship}} "{{relationship}}" "{{technology}}"{{/unless}}
{{/each}}