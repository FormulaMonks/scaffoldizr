{{properCase (removeSpaces systemName)}} = softwareSystem "{{systemName}}" {
    description "{{systemDescription}}"

    !include ../containers/{{kebabCase systemName}}.dsl
    !include ../relationships/{{kebabCase systemName}}.dsl

    tags "System"
}