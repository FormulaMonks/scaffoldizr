{{pascalCase (removeSpaces elementName)}} = container "{{{elementName}}}" "{{{containerDescription}}}" "{{{containerTechnology}}}"{{#if (eq containerType "None of the above")}}{{else}} {
    tags "{{containerType}}"
}{{/if}}