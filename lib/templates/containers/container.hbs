{{pascalCase (removeSpaces containerName)}} = container "{{containerName}}" "{{containerDescription}}" "{{containerTechnology}}"{{#if (eq containerType "None of the above")}}{{else}} {
    tags "{{containerType}}"
}{{/if}}